# Committed production defaults. Do not put secrets here.
# Supabase URL + anon key belong in your hosting provider's env settings
# (or in an uncommitted .env.local / .env.production.local for local preview).

# Keep the Dev Mode challenge hidden on the public site.
VITE_ENABLE_DEV_CHALLENGE=false
